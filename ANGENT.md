# ANGENT.md

## 目的

本文件用于约束本项目中的所有智能体和人工协作者，确保每一次代码、配置、文档、脚本和接口修改都有可追踪记录，避免出现“代码已经变了，但没人知道改了什么、为什么改、影响到哪里”的情况。

## 强制要求

从本文件生效开始，任何修改都必须在提交修改内容的同时，回填到代码记录文档中。

代码记录文档固定为：

- [docs/code-change-log.md](/Users/xiuyang/Desktop/news-caught/docs/code-change-log.md)

如果修改了代码但没有同步更新该记录文档，则该修改视为不完整。

## 适用范围

以下改动都必须记录：

- 后端代码
- 前端代码
- 配置文件
- 脚本
- 数据库模型
- API 契约
- 文档
- 测试
- 构建和运行方式

以下情况也必须记录：

- 修复 bug
- 新增功能
- 重构
- 字段改名
- 路由调整
- 样式结构调整
- 删除功能
- 临时兼容方案

## 记录时机

每次完成一个明确修改单元后，立刻更新记录文档，不要等到最后统一补写。

推荐按以下粒度记录：

- 一个功能闭环
- 一次接口调整
- 一次数据模型变化
- 一次页面交互增强
- 一次测试或脚本修复

## 记录格式

每条记录都应尽量简洁，但必须包含以下内容：

1. 日期
2. 修改人
3. 修改范围
4. 变更内容
5. 影响文件
6. 是否涉及接口/数据结构变化
7. 是否已验证
8. 风险或后续事项

## 记录要求

- 用事实描述，不写空话
- 明确写出“改了什么”，不要只写“优化了一下”
- 如果改了接口或数据结构，必须写兼容影响
- 如果只是样式或交互调整，也要写清用户可见变化
- 如果没有验证，必须明确写“未验证”

## 协作规则

- 开始较大改动前，先看最近几条变更记录，避免重复改动或冲突
- 修改完成后，先更新代码记录，再结束本轮工作
- 如果发现历史改动没有记录，补记录时要注明“补录”
- 多个智能体并行开发时，各自都要写记录，不允许互相假设别人已经写过

## 建议模板

参考 [docs/code-change-log.md](/Users/xiuyang/Desktop/news-caught/docs/code-change-log.md) 中的模板和示例。
