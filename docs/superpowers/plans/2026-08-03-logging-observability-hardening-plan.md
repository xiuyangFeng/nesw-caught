# 日志可观测性安全加固计划

日期：2026-08-03

1. 先补请求日志上下文与敏感查询脱敏失败测试。
2. 调整中间件生命周期和查询参数格式化，避免重复响应头。
3. 先更新页面卸载上报测试，再用 keepalive fetch 替换 sendBeacon。
4. 补齐独立新闻抓取入口的日志配置参数，并让 `.env.example` 可被 Git 跟踪。
5. 运行日志专项后端测试、前端 logger 测试、ruff、前端构建与 `git diff --check`。
6. 更新代码变更记录并在跨分支合并后复跑全量验证。
