# 多模块工作树集成计划

日期：2026-08-03

1. 审计五个工作树、远端状态、未提交改动和重叠文件。
2. 逐分支运行专项测试并进行代码评审；用 TDD 修复审查阻断项。
3. 将四个非空工作树分别提交，空自选股分支不创建无意义提交。
4. 创建主分支保护引用，按日志 → LLM → Latest Events → Dashboard 顺序 `--no-ff` 合并。
5. 解决变更记录和 AppShell 冲突，复跑重叠模块专项测试。
6. 执行主分支全量后端、前端、Ruff、OpenAPI 和构建验证。
7. 更新最终代码变更记录，提交集成修复并推送 `main` 到 `origin`。
