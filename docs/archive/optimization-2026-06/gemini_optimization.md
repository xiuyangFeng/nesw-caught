# 港美股消息实时平台 - 架构与规划优化建议 (Gemini 生成)

基于对当前项目的 `plan.md` 及 `docs/` 目录下各文档的阅读。总体来说，项目定位极大降低了个人开发和本地落地的隐患，目标非常清晰务实（先做基础的数据流闭环，再做AI加强和拓展）。

为了对比多家模型的看法，我从 **技术选型、存储检索方案、推送机制、AI前置落地以及工程架构** 五个维度对你的实施方向给出了以下优化建议，希望对你有所帮助：

## 1. 数据源采集的增强策略
*   **RSS 的局限性与中间件替代**：当前将原生 RSS 定位为采集主力可能存在问题，因为大部分现代财经/券商平台（雪球、富途、彭博、华尔街见闻）其实不再提供高质量或全量 RSS，甚至正文会被完全截断。
    *   **建议**：引入 [RSSHub](https://docs.rsshub.app/) 的自部署版本或直接使用社区路由，作为你的核心本地中间件。很多网站的反扒机制由 RSSHub 社区维护，你可以通过单一的 `/get_rss` 接口拉取各大站新闻，极大降低后期修抓取规则的成本。
*   **正文提取的双层兜底设计**：完全非结构化的通用库（如 `newspaper3k`、`readability`）在处理含有复杂表格、图表和弹窗拦截的财经长文时容错率偏弱。
    *   **建议**：针对最核心的 2~3 个主平台（例如雪球或 Yahoo Finance）写专用的选择器（Selector）提取逻辑，而把通用提取库作为无法匹配独立规则时的兜底（Fallback），确保关键信息不丢失。

## 2. 存储与检索增强：启用 SQLite FTS5
*   **当前盲点**：文档中提到使用 SQLite WAL 模式是一个很棒的方案。但是在文本数据（新闻标题、正文、关键词）积累后，通过 `LIKE '%keywords%'` 查询会导致全表扫描和严重卡顿。
    *   **建议**：建立表结构时，强烈建议为 `news_item` 和 `article_content` 启用 **[SQLite FTS5 (Full-Text Search) 扩展](https://www.sqlite.org/fts5.html)**。这将赋予你完全不需要外部中间件（如 Elasticsearch 或 Meilisearch）的情况下，使用本地 SQLite 达到毫秒级多关键词模糊检索、高亮、以及极大概率免锁全表匹配的能力。

## 3. 推送机制的轻量化替换：SSE (Server-Sent Events)
*   **WebSocket 的过度设计嫌疑**：文档提到系统需要单向给前端实时推送“新增新闻”、”异动”和“主题更新”。第一阶段并没有弹幕、聊天等双工交互场景。
    *   **建议**：用 **[Server-Sent Events (SSE)](https://developer.mozilla.org/zh-CN/docs/Web/API/Server-sent_events)** 代替 WebSocket。
        *   原因 1：SSE 在 HTTP 层面工作，实现极其简单，无需握手或复杂的库支持。
        *   原因 2：浏览器原生自带 **彻底的自动断线重连支持 (EventSource)** 和上次同步 ID 续传（`Last-Event-ID`），用 WebSocket 你大概率得自己在前端写一套完整的心跳保活与重试策略，开发期较长。
    
## 4. 情绪分析与主题聚合：本地 LLM 前置降纬
*   **规则模型的维护黑洞**：在“第一阶段使用轻量基线或规则，后续再靠 AI 增强”策略中，写纯规则引擎（关键词匹配）或者挂老式的中文情感分析模型，准确率往往极低且需要高频维护特定的词典（比如股市里的“暴跌”可能是空头利多，而不是绝对利空）。
    *   **建议**：考虑到这是长期*本地运行*的自用产品，现在的桌面生态非常繁荣，建议让轻量化大语言模型“早切入”。只需在本地常驻一个 **[Ollama](https://ollama.com/)**（跑类似于 Qwen2 4B 或 Llama3-8B 的小模型），并定义个简单的 JSON schema prompt 每次通过 REST API 把正文丢进去：一键同时拿到 `{"sentiment": 0.8, "keywords": [".."], "summary": ".."}`，直接取代人工撸规则。速度和质量都远胜基于库和字典的老式方案。
*   **主题聚合优化的折中**：在尚未上向量数据库（Embedding）计算相似度前的 MVP 版本，与其靠标题字符串相似度，可以通过生成基于 `Simhash` 或 `MinHash` 的局部敏感哈希以低成本实现。只要哈希指纹汉明距离足够小的，就聚合在一起，极低算力实现“去重/主题合并”。

## 5. 异动阈值的动态自适应
*   **固定绝对阈值与现实脱节**：`watchlist_item` 中的固定 `alert_threshold` 可能会在平时噪音巨大（如对 5x 杠杆标的 1% 不算动作），或在极端震荡时反应过慢。
    *   **建议**：通过行情拉取时附加一个 ATR (Average True Range，真实波动幅度的 14 日平均)，如果今天的 `价格波动量 > ATR * 1.5`，就算行情异动，这样不仅对个股、大盘、ETF 的适用度全自动兼容，不需要在配置表里为不同股票定死特定的百分比数字。 

以上是对当前规划的一些落地优化视角，期望能降低你在该产品开发过程中不必要的“业务造轮子”时间。
