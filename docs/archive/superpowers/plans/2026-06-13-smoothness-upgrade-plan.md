# 2026-06-13 新闻源与前端交互“丝滑度”优化实施计划

## 任务阶段分解

### 阶段 1: HTTP 304 缓存优化与后台正文爬虫 (后端)
- **1.1 数据库扩展**
  - 在 `source_health` 表中扩展字段 `last_etag` 与 `last_modified`。
- **1.2 改造 fetch 流程**
  - 修改 `fetcher.py` 和 `news_ingestion.py`，支持带缓存标志的 `If-None-Match` 和 `If-Modified-Since` 请求头。
  - 处理 304 状态码逻辑，更新最新的缓存标头。
- **1.3 实现通用正文提取异步爬虫**
  - 编写 `app/services/ingestion/article_crawler.py`。
  - 编写解析提取策略，过滤干扰性标签。
  - 对没有正文的入库新闻加入异步后台队列处理。
- **1.4 单元测试**
  - 编写对 304 缓存控制和正文智能抽取的 pytest 测试（TDD）。

### 阶段 2: 1:1 呼吸骨架屏与转场动画 (前端)
- **2.1 编写 `SkeletonFeed.vue`**
  - 设计骨架结构和渐变闪烁动画（Shimmer effect）。
- **2.2 集成转场动效**
  - 在 `NewsFeedView.vue`, `DashboardView.vue`, `WatchlistView.vue` 引入骨架占位。
  - 实现基于 `opacity` 的交叉平滑淡入淡出。

### 阶段 3: AI 对话打字机与智能滚动锁定 (前端)
- **3.1 实现字符缓动吐字缓冲区**
  - 对流式数据包的前端响应进行微秒级缓动切片，消除块级文字跳跃。
- **3.2 编写滚动锁及浮动置底按钮**
  - 在 `ChatView.vue` 中绑定滚动视口事件。
  - 智能切换自动滚动锁，并在适当时机悬浮弹出“回到底部”毛玻璃按钮。

### 阶段 4: 增量提示与点击置入 (前端)
- **4.1 引入增量提醒**
  - 监听 SSE 里的增量入库推送。
  - 在顶部动态展现提示浮条。
- **4.2 动效置入**
  - 点击更新后，通过过渡动画将新数据平滑推入列表顶部。
